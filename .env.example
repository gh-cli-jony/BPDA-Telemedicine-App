# BPDA Telemedicine - Frontend Environment Variables
# Copy this file to .env and set your production values before building.

# Root URL of the backend API (no trailing slash).
# The built frontend will use this unless overridden at runtime via public/config.js.
VITE_API_BASE_URL=https://your-domain.com/api
